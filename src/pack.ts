import type { Package } from '@mnrendra/read-package'

/**
 * `package.json` pack.
 */
const pack: Package = {
  // name
  name: 'package-name',
  // version
  version: '0.0.0'
}

export default pack
